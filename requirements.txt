# Pinned dependencies for the lake model scripts in src/ (numpy/pandas
# reference model: processBased_lakeModel_functions.py, run_M3.py,
# plot_output.py; JAX autodiff port: jax_lakeModel_functions.py,
# run_M3_jax.py, calibrate_M3_jax.py).
#
# This replaces a previous requirements.txt that was a full `pip freeze`
# of an unrelated local Anaconda environment (local file:// paths tied to
# one machine, hundreds of unrelated packages, and missing jax/jaxlib/
# optax) -- not usable to set up this project elsewhere. The versions
# below were verified together: all project imports succeed, the full
# test suite passes, and a calibration run completes end-to-end.
#
# Setup (any computer): see setup_env.sh, or manually:
#   python3 -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt

numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
matplotlib==3.10.9
seaborn==0.13.2
numba==0.67.0
tqdm==4.70.0

# JAX autodiff stack (CPU wheels; for GPU/TPU see
# https://docs.jax.dev/en/latest/installation.html for the right
# jax[cuda12]/jax[tpu] extra for your machine instead of plain jaxlib)
jax==0.6.2
jaxlib==0.6.2
optax==0.2.8

# testing
pytest
